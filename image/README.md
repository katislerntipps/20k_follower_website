# Image Assets

Place required image assets in this directory:

- `logo.png` – Site logo displayed in the header across pages.
- `timer_tree.png` – Tree illustration used on the home and timer pages.
- `background_tree.png` – Background artwork referenced by the main stylesheet.

Add the files with these exact names so the HTML and CSS references resolve correctly.
